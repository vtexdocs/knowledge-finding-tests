# Issue: Create budgets via API

| Field | Value |
|-------|--------|
| **issue_id** | budgets-api-01 |
| **persona** | Developer |
| **product** | Budgets / B2B |
| **user_intent** | How to create budgets via API for B2B contexts. |
| **target_doc_url** | https://developers.vtex.com/docs/api-reference/budgets-api#post-/api/budgets/-contextType-/-contextId-?endpoint=post-/api/budgets/-contextType-/-contextId- |
| **surface** | developers-portal |
| **target_docs** | ["https://developers.vtex.com/docs/api-reference/budgets-api#post-/api/budgets/-contextType-/-contextId-?endpoint=post-/api/budgets/-contextType-/-contextId-"] |
| **other_helpful_docs** | ["https://developers.vtex.com/docs/guides/budgets-and-allocations"] |
| **source** | Command input (target document URL) |

> **Field descriptions:**
> - **target_docs**: All target-doc URLs that effectively solve the user issue (array of strings).
> - **other_helpful_docs**: Doc URLs that partially address the issue, or contain helpful information AND point to a target doc (array of strings).

---

## Queries by path

### A — External search (Google)

| locale | style | query |
|--------|-------|-------|
| en | naive | how to create budgets in vtex using the api |
| en | familiar | create budget api vtex |
| en | expert | budgets api create budget vtex |

**Array (query_external):**
```json
[
  { "locale": "en", "style": "naive", "query": "how to create budgets in vtex using the api" },
  { "locale": "en", "style": "familiar", "query": "create budget api vtex" },
  { "locale": "en", "style": "expert", "query": "budgets api create budget vtex" }
]
```

### B — Internal search (Algolia / Proprietary API)

| locale | style | query |
|--------|-------|-------|
| en | naive | budget api create |
| en | familiar | create budget |
| en | expert | budgets api |

**Array (query_internal):**
```json
[
  { "locale": "en", "style": "naive", "query": "budget api create" },
  { "locale": "en", "style": "familiar", "query": "create budget" },
  { "locale": "en", "style": "expert", "query": "budgets api" }
]
```

### C — Docs assistant API (MCP-backed)

| locale | style | query |
|--------|-------|-------|
| en | naive | vtex docs for creating budgets via api |
| en | familiar | vtex budgets api create |
| en | expert | budgets api |

**Array (query_mcp):**
```json
[
  { "locale": "en", "style": "naive", "query": "vtex docs for creating budgets via api" },
  { "locale": "en", "style": "familiar", "query": "vtex budgets api create" },
  { "locale": "en", "style": "expert", "query": "budgets api" }
]
```

### D — External LLMs

| locale | style | query |
|--------|-------|-------|
| en | naive | how do i create a budget using vtex api |
| en | familiar | what api endpoint creates budgets in vtex |
| en | expert | how to create budgets via vtex budgets api |

**Array (query_llm):**
```json
[
  { "locale": "en", "style": "naive", "query": "how do i create a budget using vtex api" },
  { "locale": "en", "style": "familiar", "query": "what api endpoint creates budgets in vtex" },
  { "locale": "en", "style": "expert", "query": "how to create budgets via vtex budgets api" }
]
```


